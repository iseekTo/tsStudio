/**
 * if add input onChange event
 * you can do this
 * if env is React
 * We can distinguish different values according to the name attribute of input.
 */
// let handleOnchange = (
//     val: string,
//     event: React.HandlerChange<HTMLInputElement>,
// ) => {
//     let composeInput: object = {}
//     composeInput[event.target.name] = val
//     this.setState(composeInput)
// }
